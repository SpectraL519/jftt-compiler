# Simple := operator test
PROGRAM IS
    a, b, c, t[5]
IN
    READ a;
    WRITE a; # in

    b := 3;
    WRITE b; # 3

    c := a;
    WRITE c; # in

    t[0] := a;
    WRITE t[0]; # in

    t[b] := 5;
    WRITE t[b]; # 5

    a := t[b];
    WRITE a; # 5
END
